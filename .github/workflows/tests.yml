name: Hunt_Oracle_ARM

on:
  workflow_dispatch:
  schedule:
    - cron: '*/15 * * * *' # 15 phút đấm 1 phát cho đỡ bị khóa ní nhé
  push:
    branches: [ main ]

jobs:
  hunt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          
      - name: Install dependencies
        run: composer install --prefer-dist --no-progress
        
      - name: Create Private Key File
        run: |
          echo "${{ secrets.OCI_SSH_PRIVATE_KEY_CONTENTS }}" > private_key.pem
          chmod 600 private_key.pem

      - name: Run Hunt Bot
        run: |
          vendor/bin/phpunit --color tests/
        env:
          OCI_REGION: ${{ secrets.OCI_REGION }}
          OCI_USER_ID: ${{ secrets.OCI_USER_OCID }}
          OCI_TENANCY_ID: ${{ secrets.OCI_TENANCY_OC }}
          OCI_KEY_FINGERPRINT: ${{ secrets.OCI_FINGERPRINT }}
          OCI_PRIVATE_KEY_FILENAME: ./private_key.pem
          OCI_SSH_PUBLIC_KEY: ${{ secrets.OCI_SSH_PUBLIC_KEY }}
          # Cấu hình "xe con" 2/18 cho dễ lọt khe Singapore nè
          OCI_AVAILABILITY_DOMAIN: "ap-singapore-1-AD-1"
          OCI_SHAPE: "VM.Standard.A1.Flex"
          OCI_OCPUS: 2
          OCI_MEMORY_IN_GBS: 18
          OCI_MAX_INSTANCES: 1
